select
    player_id,
    matches_played,
    total_goals,
    total_assists,
    total_minutes_played,
    total_yellow_cards,
    total_red_cards,
    avg_minutes_per_match,
    goals_per_match,
    assists_per_match,
    goals_per_90,
    assists_per_90

from {{ ref('int_player_performance_summary') }}